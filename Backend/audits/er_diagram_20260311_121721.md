```mermaid
erDiagram
    equipes {
        INTEGER id PK
        TEXT nom
    }
    resultats {
        INTEGER id PK
        INTEGER journee
        INTEGER equipe_dom_id
        INTEGER equipe_ext_id
        INTEGER score_dom
        INTEGER score_ext
    }
    cotes {
        INTEGER id PK
        INTEGER journee
        INTEGER equipe_dom_id
        INTEGER equipe_ext_id
        DECIMAL(5,2) cote_1
        DECIMAL(5,2) cote_x
        DECIMAL(5,2) cote_2
    }
    classement {
        INTEGER id PK
        INTEGER journee
        INTEGER equipe_id
        INTEGER position
        INTEGER points
        TEXT forme
        DECIMAL(4,2) buts_pour
        DECIMAL(4,2) buts_contre
    }
    predictions {
        INTEGER id PK
        INTEGER journee
        INTEGER equipe_dom_id
        INTEGER equipe_ext_id
        TEXT prediction
        TEXT resultat
        DECIMAL(5,2) fiabilite
        INTEGER succes
        INTEGER points_gagnes
    }
    score_ia {
        INTEGER id PK
        DECIMAL(10,2) score
        INTEGER predictions_total
        INTEGER predictions_reussies
        INTEGER pause_until
        INTEGER session_archived
        TEXT derniere_maj
    }
    classement_global {
        INTEGER id PK
        INTEGER journee
        INTEGER position
        INTEGER equipe_id
        INTEGER points
        TEXT forme
    }
    matches_global {
        INTEGER id PK
        INTEGER journee
        INTEGER equipe_dom_id
        INTEGER equipe_ext_id
        DECIMAL(5,2) cote_1
        DECIMAL(5,2) cote_x
        DECIMAL(5,2) cote_2
        TEXT status
        INTEGER score_dom
        INTEGER score_ext
    }
    sessions {
        INTEGER session_id PK
        TIMESTAMP timestamp_debut
        TIMESTAMP timestamp_fin
        INTEGER capital_initial
        INTEGER capital_final
        INTEGER nombre_journees
        TEXT version_ia
        INTEGER profit_total
        TEXT type_session
        INTEGER score_zeus
    }
    historique_paris {
        INTEGER id_pari PK
        INTEGER session_id
        INTEGER match_id
        INTEGER journee
        TEXT type_pari
        INTEGER mise_ar
        REAL pourcentage_bankroll
        REAL cote_jouee
        INTEGER resultat
        INTEGER profit_net
        INTEGER bankroll_apres
        TIMESTAMP timestamp_pari
        REAL probabilite_implicite
        INTEGER action_id
    }
    resultats |o--o{ equipes : "equipe_ext_id -> id"
    resultats |o--o{ equipes : "equipe_dom_id -> id"
    cotes |o--o{ equipes : "equipe_ext_id -> id"
    cotes |o--o{ equipes : "equipe_dom_id -> id"
    classement |o--o{ equipes : "equipe_id -> id"
    predictions |o--o{ equipes : "equipe_ext_id -> id"
    predictions |o--o{ equipes : "equipe_dom_id -> id"
    classement_global |o--o{ equipes : "equipe_id -> id"
    matches_global |o--o{ equipes : "equipe_ext_id -> id"
    matches_global |o--o{ equipes : "equipe_dom_id -> id"
    historique_paris |o--o{ matches_global : "match_id -> id"
    historique_paris |o--o{ sessions : "session_id -> session_id"
```
